#include "monty.h"
#include <stdio.h>

/**
 * nop - Function that prints nothing
 * @head: Double pointer to the top of the stack
 * @number: Line number of the command
 */

void nop(stack_t **head, unsigned int number)
{
	(void)head;
	(void)number;
}
